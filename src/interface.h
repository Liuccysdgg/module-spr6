#pragma once

#include <vector>
#include <array>
#include <string> 
//std::vector<std::array<unsigned char, 32>> spr6_make(const std::string& username, const std::string& password);
//bool spr6_check_password(const std::string& username, const std::string& password, std::array<unsigned char, 32> salt, std::array<unsigned char, 32> verifier);